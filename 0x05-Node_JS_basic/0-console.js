/**
 * Prints a strig to the STDOUT.
 * @param {String} msg The string to display.
 * @author Abdulkadir Badamasi <https://github.com/abmahuta>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
